# SwiftPM (commit Package.resolved when generated — it pins dependencies)
.build/
.swiftpm/

# Xcode (the app project AND Info.plist are generated by XcodeGen —
# `xcodegen generate` in ArgusApp/; never commit them)
*.xcodeproj/
ArgusApp/Info.plist
xcuserdata/
DerivedData/

# macOS
.DS_Store
